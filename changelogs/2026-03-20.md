🗓 2026-03-20 · 11:17 (GMT+2)

🔹 Request

User requested that the new bare SwiftUI detail style be added to the project design rules.

🔧 Changes Made
	•	Updated `DESIGN_RULES.md` to make plain grouped SwiftUI sections the default for detail screens.
	•	Added explicit guidance to avoid heavy custom cards, pills, glass effects, and decorative chrome unless there is a strong functional reason.
	•	Added a dedicated `Detail View Standard` section covering native rows, plain section headers, and restrained action styling.

Files modified:
	•	DESIGN_RULES.md

✅ Result

The preferred UI direction is now documented: detail screens should default to native SwiftUI list and section styling instead of custom card-heavy treatments.

⸻

🗓 2026-03-20 · 11:10 (GMT+2)

🔹 Request

User requested that `WorkoutDetailView` drop the custom cards and styling and return to bare basic SwiftUI elements.

🔧 Changes Made
	•	Rebuilt `WorkoutDetailView` around plain inset-grouped SwiftUI sections instead of custom card containers and bespoke surface styling.
	•	Removed custom pills, stat tiles, custom section headers, and custom primary button styling from the detail screen.
	•	Kept the shared route modal and the existing workout actions while simplifying the main detail presentation back to standard SwiftUI rows and buttons.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

`WorkoutDetailView` now uses a much more basic native SwiftUI presentation, without the custom card treatment that had been layered onto it.

⸻

🗓 2026-03-20 · 11:04 (GMT+2)

🔹 Request

User requested that the card treatment in `WorkoutDetailView` change because the existing heavy card styling did not fit the newer UI direction.

🔧 Changes Made
	•	Reworked `WorkoutDetailView` from a stacked glass-card layout into grouped SwiftUI sections using inset list styling.
	•	Removed the heavier standalone card containers from the main detail sections so the system section chrome carries more of the layout.
	•	Kept the updated content hierarchy while shifting the overall screen presentation closer to the newer native list/form surfaces used elsewhere in the app.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

`WorkoutDetailView` now uses a cleaner grouped-section layout instead of a stack of prominent custom cards, which should feel closer to the newer app screens.

⸻

🗓 2026-03-20 · 11:05 (GMT+2)

🔹 Request

User clarified that the mismatch in `WorkoutDetailView` was coming from the basic SwiftUI elements and navigation chrome rather than the card content styling.

🔧 Changes Made
	•	Updated `WorkoutDetailView` to use a large navigation title instead of the older inline modal-style header.
	•	Replaced the labeled overflow action with the same icon-led trailing toolbar pattern used in newer screens.
	•	Removed the extra `Done` button so the screen relies on standard navigation behavior instead of older modal chrome.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The base SwiftUI navigation and toolbar elements in `WorkoutDetailView` now feel more consistent with `WorkoutListView` and the app’s newer screens.

⸻

🗓 2026-03-20 · 10:59 (GMT+2)

🔹 Request

User requested that `WorkoutDetailView` be updated to match the newer UI design language used elsewhere in the app, especially `WorkoutListView`.

🔧 Changes Made
	•	Refreshed the workout detail header into a cleaner summary card with compact metadata pills for faster scanning.
	•	Updated stats, notes, route, and exercises to share a more consistent heading and supporting-text hierarchy.
	•	Converted stats into compact tiled cards and refined the route and exercise headers so the detail screen matches the app’s newer visual rhythm more closely.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The workout detail screen now feels more aligned with `WorkoutListView`, with clearer hierarchy, tighter metadata presentation, and more modern section styling.

⸻

🗓 2026-03-20 · 13:34 (GMT+2)

🔹 Request

User requested that only exercises linked to the selected workout type should show up under `Add Exercises`.

🔧 Changes Made
	•	Updated `AddWorkoutView` to filter available exercise subcategories by the currently selected Apple workout activity, with broad workout-type fallback for older category data.
	•	Updated `EditWorkoutView` to use the same filtered subcategory source so the edit flow matches the add flow.
	•	Because the exercise picker and quick-add options already derive from those filtered subcategories, the `Add Exercises` library now automatically limits itself to exercises linked to the current workout type.

Files modified:
	•	TrainState/Views/AddWorkoutView.swift
	•	TrainState/Views/EditWorkoutView.swift

✅ Result

`Add Exercises` now only shows exercises from subcategories linked to the currently selected Apple workout type instead of showing the full exercise library.

⸻

🗓 2026-03-20 · 13:28 (GMT+2)

🔹 Request

User requested that categories should link with Apple workout types instead of only the app’s broad workout buckets.

🔧 Changes Made
	•	Extended `WorkoutCategory` to store an exact Apple workout activity reference while keeping a compatibility `workoutType` view derived from that activity.
	•	Updated category export/import restore logic so category backups now preserve the Apple workout activity linkage.
	•	Changed category assignment to filter and create categories using the workout’s saved Apple workout activity, with broad-type fallback so older categories still remain visible.
	•	Updated category-management grouping and analytics labels to surface the linked Apple workout activity name.
	•	Adjusted default seeded categories so newly initialized category data is created against representative Apple workout activities.

Files modified:
	•	TrainState/Models/WorkoutCategory.swift
	•	TrainState/Models/Workout.swift
	•	TrainState/Managers/CloudKitManager.swift
	•	TrainState/Managers/DataInitializationManager.swift
	•	TrainState/Views/CategorySelection/CategoryAndSubcategorySelectionView.swift
	•	TrainState/Views/CategoriesManagementView.swift
	•	TrainState/Views/WorkoutListView.swift
	•	TrainState/Views/AnalyticsView.swift

✅ Result

Categories now track Apple workout activities directly, new category assignment follows the exact Apple workout type on the workout, and older category data still resolves through broad-type compatibility until it is updated.

⸻

🗓 2026-03-20 · 13:12 (GMT+2)

🔹 Request

User reported that the Apple workout type dropdown on the edit workout screen was not prefilling for workouts that already had an Apple workout type saved.

🔧 Changes Made
	•	Added a shared `AppleWorkoutSelection.normalized` helper to align saved Apple workout activity/location values with the actual picker option tags.
	•	Updated `EditWorkoutView` to initialize its Apple workout picker from the normalized saved selection so location-aware activities like indoor/outdoor running or cycling resolve to a valid menu item.

Files modified:
	•	TrainState/Extensions/HKWorkoutActivityType+WorkoutType.swift
	•	TrainState/Views/EditWorkoutView.swift

✅ Result

Existing Apple workout-based workouts now open in edit mode with the Apple workout type dropdown correctly prefilled instead of appearing unset.

⸻

🗓 2026-03-20 · 10:54 (GMT+2)

🔹 Request

User requested that both route map modals use the same UI instead of having two different presentations.

🔧 Changes Made
	•	Extracted a shared `RouteMapSheetView` so workout-detail and workout-list route entry points now use the same modal implementation.
	•	Standardized the map-style picker, playback control, dismissal behavior, and playback reset logic across both route sheets.
	•	Removed duplicate route modal state from `WorkoutListView` now that the shared sheet manages its own state.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Both route map modals now behave and look the same, so opening a route from either screen gives a consistent map experience.

⸻

🗓 2026-03-20 · 10:54 (GMT+2)

🔹 Request

User requested that the route playback control not sit in its own separate area.

🔧 Changes Made
	•	Moved the workout-detail route playback control out of the standalone top row and into the navigation bar toolbar.
	•	Aligned the route detail presentation more closely with the compact control style already used in the workout-list route preview.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The route playback action now feels integrated with the rest of the map UI instead of occupying a dedicated control band above the map.

⸻

🗓 2026-03-20 · 10:52 (GMT+2)

🔹 Request

User reported that the playback icon was visible again but moved in a jumpy way.

🔧 Changes Made
	•	Removed the manual on-screen marker animation that was conflicting with MapKit’s annotation positioning.
	•	Replaced the playback marker symbol with a compact custom annotation dot for smoother route playback movement.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift

✅ Result

The playback marker now follows the route with steadier motion instead of jumping as MapKit re-lays out the annotation.

⸻

🗓 2026-03-20 · 10:48 (GMT+2)

🔹 Request

User reported that the playback indicator was no longer visible at all after the previous map playback adjustment.

🔧 Changes Made
	•	Replaced the zoom-scaled playback overlay with a fixed-size playback annotation so the indicator remains visible regardless of map zoom level.
	•	Updated the playback animation path to move the annotation view directly on screen while still tracking the saved route coordinate.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift

✅ Result

The playback marker should now remain visible on the map and continue animating without requiring manual interaction or a specific zoom level.

⸻

🗓 2026-03-20 · 10:46 (GMT+2)

🔹 Request

User reported that route playback would not visibly start until the map was manually zoomed or moved.

🔧 Changes Made
	•	Reworked the route playback indicator in `RouteMapView` to use a dedicated map overlay instead of a moving annotation.
	•	Updated playback position rendering so each timer tick forces an immediate map redraw without requiring additional user interaction.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift

✅ Result

Route playback now begins rendering immediately after pressing play, without needing the user to pan or zoom the map first.

⸻

🗓 2026-03-20 · 10:33 (GMT+2)

🔹 Request

User requested slower route playback and asked for stop to be replaced with pause that resumes from the paused position.

🔧 Changes Made
	•	Adjusted route playback timing to run more slowly for easier visual tracking.
	•	Changed map playback controls from stop to pause in both route map presentations.
	•	Updated `RouteMapView` playback state so pausing preserves the current route position and resuming continues from that saved point.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Route playback now moves at a calmer pace, and pausing no longer restarts the animation, which makes route review feel more controlled and predictable.

⸻

🗓 2026-03-20 · 10:27 (GMT+2)

🔹 Request

User requested a button on the route map that plays back the route.

🔧 Changes Made
	•	Added route playback support to `RouteMapView`, including an animated progress marker that moves along the recorded path.
	•	Added play/stop controls to the workout-detail route sheet and the workout-list route preview sheet.
	•	Ensured playback state resets cleanly when the animation finishes or the route sheet is dismissed.

Files modified:
	•	TrainState/Views/Components/RouteMapView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Users can now start a simple route playback directly from the map UI, making it easier to review the path progression of a recorded workout.

⸻

🗓 2026-03-20 · 13:00 (GMT+2)

🔹 Request

User asked to fix the date rendering on the workout list view.

🔧 Changes Made
	•	Replaced the workout row date chip with a plain tertiary date label so the list row reads more cleanly.
	•	Removed the custom stacked month/day/year treatment and pill styling that made the date feel too heavy in the list.

Files modified:
	•	TrainState/Views/Components/WorkoutRowView.swift

✅ Result

Workout dates in the list now render as simple supporting text instead of a styled chip, which keeps the focus on the workout content and section headers.
