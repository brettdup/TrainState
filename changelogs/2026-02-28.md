🗓 2026-02-28 · 14:18 (GMT+2)

🔹 Request

User requested a new categories tab area in the last-trained screen that lists subcategories by most recent training, with each row opening its history, and asked for the screen to be reachable from the navigation bar.

🔧 Changes Made
	•	Added a segmented tab control to the Strength Last Trained screen so users can switch between category and exercise lists.
	•	Converted strength subcategory rows into navigation links while keeping the list sorted by last trained date.
	•	Added a subcategory history detail view that shows matching workout sessions and links into each workout’s full detail screen.
	•	Added a navigation bar shortcut in Analytics so the Last Trained screen can be opened directly from the top bar.
	•	Added an exercise list inside each subcategory history view so users can drill into a specific movement from that subcategory.
	•	Expanded exercise insights with individual logged entries, total entry counts, and a “max reps at max weight” summary alongside the existing graph and progression stats.

Files modified:
	•	TrainState/Views/SubcategoryLastLoggedView.swift
	•	TrainState/Views/AnalyticsView.swift
	•	TrainState/Views/ExerciseInsightsView.swift

✅ Result

The last-trained area now has a dedicated categories tab where subcategories are easy to browse by recency, each one opens a drill-in history view for past sessions, exercises inside that subcategory can be opened for full per-entry analysis, and the screen is available directly from the Analytics navigation bar.

⸻

🗓 2026-02-28 · 12:45 (GMT+2)

🔹 Request

User reported an App Store validation error because the widget extension build number did not match the containing app build number.

🔧 Changes Made
	•	Updated the widget extension target build number from `1` to `3` in both Debug and Release configurations.

Files modified:
	•	TrainState.xcodeproj/project.pbxproj

✅ Result

The widget extension now matches the parent app’s `CFBundleVersion`, resolving the bundle version mismatch during archive validation.
