🗓 2026-04-06 · 08:37 (GMT+2)

🔹 Request

User reported that outdoor running workouts no longer showed the map/route option.

🔧 Changes Made
• Updated bulk HealthKit import to fetch and attach route data per imported workout instead of only importing summary workout fields.
• Converted `importWorkoutsBatch` to async so route fetching can run during bulk import.
• Updated the bulk import caller to await the new async batch import path.

Files modified:
• TrainState/Services/HealthKitRecentWorkoutImporter.swift
• TrainState/Views/WorkoutListView.swift

✅ Result

Workouts imported via “Import All” now include route data when available, so outdoor runs can show the map/route option again.
