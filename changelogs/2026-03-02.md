🗓 2026-03-02 · 14:12 (GMT+2)

🔹 Request

User chose to match the edit workout screen to the workout detail screen by showing generated set rows from summary fields when needed.

🔧 Changes Made
• Restored summary-based fallback set rendering for `ExerciseLogEntry` cards in the shared exercise card component.
• Kept the richer per-set display as the preferred source when actual set entries exist.

Files modified:
• TrainState/Views/Components/ExerciseCardView.swift

✅ Result

Edit workout now shows the same visible set rows as the workout detail screen before opening the exercise editor, keeping both screens consistent.

🗓 2026-03-02 · 14:05 (GMT+2)

🔹 Request

User reported that the workout detail screen showed per-set information, but the edit workout screen did not.

🔧 Changes Made
• Updated workout edit loading to rebuild `setEntries` from the saved exercise notes so real per-set data is available in the editor.
• Updated edit autosave to persist per-set notes back onto `WorkoutExercise` records.
• Expanded the edit snapshot tracking so autosave detects per-set note changes as well.

Files modified:
• TrainState/Views/EditWorkoutView.swift

✅ Result

The edit workout screen now renders the same saved per-set exercise data as the workout detail screen, and edits to that set data persist correctly.

🗓 2026-03-02 · 13:56 (GMT+2)

🔹 Request

User requested that exercise cards should not synthesize visible set rows from summary values.

🔧 Changes Made
• Removed the fallback set-row generation from the shared exercise card.
• Kept card rendering limited to actual per-set entries only.

Files modified:
• TrainState/Views/Components/ExerciseCardView.swift

✅ Result

Exercise cards no longer invent set rows from summary fields, preserving a strict display of only real logged set data.

🗓 2026-03-02 · 13:52 (GMT+2)

🔹 Request

User reported that exercise cards only showed set details after opening the exercise editor.

🔧 Changes Made
• Updated the shared exercise card to generate visible fallback set rows from stored sets, reps, and weight values when per-set entries are not present.
• Preserved the richer per-set display when detailed set entries are available.

Files modified:
• TrainState/Views/Components/ExerciseCardView.swift

✅ Result

Exercise cards now show set information directly in the list, so users can review workout structure without opening each exercise.

🗓 2026-03-02 · 13:40 (GMT+2)

🔹 Request

User requested the exercise-adding experience to be shared between workout editing and live workout mode, with matching drag-and-drop and custom exercise support, and for workout edits to persist automatically without a save button.

🔧 Changes Made
• Consolidated the exercise management UI into a shared reusable section used by both edit and live strength flows.
• Kept exercise search, custom exercise creation, and drag-to-reorder behavior aligned across both contexts.
• Switched workout editing to autosave changes as fields and exercises change, removing the dedicated save action.

Files modified:
• TrainState/Views/EditWorkoutView.swift
• TrainState/Views/LiveStrengthSessionView.swift
• TrainState/Views/Components/WorkoutExerciseSectionView.swift

✅ Result

Exercise management is now maintained from one shared view, reducing drift between edit and live flows, and workout edits persist automatically as the user makes changes.
