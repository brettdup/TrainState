🗓 2026-03-23 · 15:40 (GMT+2)

🔹 Request

User asked to delete files that are no longer used.

🔧 Changes Made
	•	Removed unused picker and chip components that no longer have any inbound references after the exercise and category UI refactors.
	•	Deleted unused layout and background helper views that were only left behind with preview-only references.
	•	Removed the unused `NetworkManager` utility and the stray `.DS_Store` file inside the asset catalog.

Files modified:
	•	TrainState/Views/Components/ExerciseOptionPickerView.swift
	•	TrainState/Views/Components/CategoryChip.swift
	•	TrainState/Views/Components/SubcategoryChip.swift
	•	TrainState/Views/Components/WrappingHStackLayout.swift
	•	TrainState/Views/BackgroundView.swift
	•	TrainState/Views/Components/CategoryFilterChipsView.swift
	•	TrainState/Utilities/NetworkManager.swift
	•	TrainState/Assets.xcassets/.DS_Store

✅ Result

The project is now cleaner and no longer carries the obviously dead SwiftUI components and utility file that were left behind after recent UI changes.

⸻

🗓 2026-03-23 · 14:35 (GMT+2)

🔹 Request

User rejected the previous add-exercises redesign and asked for a different direction.

🔧 Changes Made
	•	Reworked `UnifiedExercisePickerView` away from the bottom add bar and summary-row pattern.
	•	Switched the subcategory control to a simpler native menu-style filter row instead of the chip-heavy presentation.
	•	Restored the picker to a more standard searchable list flow with a normal toolbar add action and lighter create-custom row styling.

Files modified:
	•	TrainState/Views/Components/UnifiedExercisePickerView.swift

✅ Result

The add-exercises screen now feels closer to a standard iOS selection sheet, with less chrome and a simpler filtering and selection flow.

⸻

🗓 2026-03-23 · 14:28 (GMT+2)

🔹 Request

User requested that the add-exercises screen itself should look nicer.

🔧 Changes Made
	•	Refined `UnifiedExercisePickerView` to use a cleaner native grouped-list layout with a clearer filter section and summary row.
	•	Moved the primary add action into a bottom selection bar so the screen reads more like a deliberate multi-select flow.
	•	Updated `ExerciseRowView` with cleaner icon treatment, better text hierarchy, and wrapping titles for longer exercise names.

Files modified:
	•	TrainState/Views/Components/UnifiedExercisePickerView.swift
	•	TrainState/Views/Components/ExerciseRowView.swift

✅ Result

The add-exercises picker now has clearer structure, cleaner rows, and a more polished selection flow than the previous plain list-plus-toolbar presentation.

⸻

🗓 2026-03-23 · 14:15 (GMT+2)

🔹 Request

User requested that subcategories should display more compactly and fit on one line instead of taking up too much vertical space.

🔧 Changes Made
	•	Updated the workout-detail category section in `WorkoutDetailView` so subcategories now render as a single compact metadata line instead of separate rows.
	•	Kept categories as distinct rows while reducing the vertical footprint of the more numerous subcategory list.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Subcategories now take up much less space on the workout detail screen and read as a compact summary instead of a long stacked list.

⸻

🗓 2026-03-23 · 14:08 (GMT+2)

🔹 Request

User requested that the muscle map feature should be removed entirely.

🔧 Changes Made
	•	Removed the muscle map entry point from `CategoryAndSubcategorySelectionView`.
	•	Deleted `MuscleMapSelectionView` and removed the imported SVG asset from the asset catalog.
	•	Cleaned up the now-unused muscle-map asset folders so the category flow returns to the simpler native list-only implementation.

Files modified:
	•	TrainState/Views/CategorySelection/CategoryAndSubcategorySelectionView.swift
	•	TrainState/Views/CategorySelection/MuscleMapSelectionView.swift
	•	TrainState/Assets.xcassets/MusclesFrontBack.imageset/Contents.json
	•	TrainState/Assets.xcassets/MusclesFrontBack.imageset/muscles-front-back.svg

✅ Result

The muscle map has been fully removed, leaving the category selection flow on the cleaner native list path without the abandoned anatomy picker.

⸻

🗓 2026-03-23 · 14:00 (GMT+2)

🔹 Request

User reported that the muscle-map overlay was not aligning correctly on the real SVG anatomy diagram.

🔧 Changes Made
	•	Updated `MuscleMapSelectionView` to position overlay regions against the actual fitted SVG image rect instead of the old placeholder-based crop math.
	•	Changed the front/back toggle behavior to use the real combined front/back artwork geometry and highlight the active half while keeping the full diagram visible.

Files modified:
	•	TrainState/Views/CategorySelection/MuscleMapSelectionView.swift

✅ Result

The muscle-map overlay now aligns against the real SVG anatomy asset instead of drifting from the intended muscle regions.

⸻

🗓 2026-03-23 · 13:48 (GMT+2)

🔹 Request

User requested that the edit-categories control should match the add-exercises styling and asked for a muscle map using a found or generated diagram asset.

🔧 Changes Made
	•	Updated the workout-detail category action in `WorkoutDetailView` to match the flatter native row style used by the add-exercises affordance.
	•	Researched and imported a real Wikimedia Commons SVG muscle diagram asset into `Assets.xcassets`.
	•	Implemented a new `MuscleMapSelectionView` as a strength-focused alternate picker for category selection.
	•	Wired the muscle map into `CategoryAndSubcategorySelectionView` so strength workouts can select matching subcategories from the real front/back diagram while still using the existing category and subcategory model.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/CategorySelection/CategoryAndSubcategorySelectionView.swift
	•	TrainState/Views/CategorySelection/MuscleMapSelectionView.swift
	•	TrainState/Assets.xcassets/MusclesFrontBack.imageset/Contents.json
	•	TrainState/Assets.xcassets/MusclesFrontBack.imageset/muscles-front-back.svg

✅ Result

Strength category selection now includes a first-pass native SwiftUI muscle map backed by a real SVG anatomy asset, and the category action styling is visually aligned with the rest of the screen.

⸻

🗓 2026-03-23 · 13:35 (GMT+2)

🔹 Request

User requested that the `Add Exercises` affordance should look nicer and not use the current background treatment.

🔧 Changes Made
	•	Removed the tinted rounded background styling from the `Add Exercises` button in `WorkoutDetailView`.
	•	Updated the shared `WorkoutExerciseSectionView` version of the same control to match the flatter native row-style appearance.
	•	Switched the icon from the filled plus-circle variant to the lighter outlined version for a cleaner look.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/Components/WorkoutExerciseSectionView.swift

✅ Result

The `Add Exercises` action now feels cleaner and more native, without the heavier colored background block.

⸻

🗓 2026-03-23 · 13:29 (GMT+2)

🔹 Request

User requested that the exercise rows should not have borders and that long exercise titles should wrap instead of going off screen.

🔧 Changes Made
	•	Removed the bordered card background treatment from the shared `ExerciseCardView`.
	•	Updated exercise name text in `ExerciseCardView` to wrap onto multiple lines instead of truncating.

Files modified:
	•	TrainState/Views/Components/ExerciseCardView.swift

✅ Result

Exercise rows now feel lighter and more native, and long exercise names remain fully readable instead of being cut off.

⸻

🗓 2026-03-23 · 13:22 (GMT+2)

🔹 Request

User requested that the `WorkoutDetailView` preview should represent a realistic strength training workout with multiple categories, subcategories, exercises, and sets.

🔧 Changes Made
	•	Replaced the old minimal running preview in `WorkoutDetailView` with a seeded strength training preview.
	•	Added 2 workout categories and 7 linked subcategories to better exercise the classification UI.
	•	Added multiple strength exercises with populated set, rep, weight, and per-set note data so the preview reflects a realistic logged session.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The `WorkoutDetailView` preview now shows a much more representative strength workout state, making it easier to evaluate the screen’s layout and hierarchy during development.

⸻

🗓 2026-03-23 · 13:16 (GMT+2)

🔹 Request

User requested that the workout-detail category section should feel more native.

🔧 Changes Made
	•	Removed the remaining custom grouped styling from the category and subcategory display in `WorkoutDetailView`.
	•	Changed the detail-screen classification display to plain SwiftUI list rows with standard section-style labels and symbols.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The workout-detail category area now reads more like native SwiftUI list content and less like a custom embedded component.

⸻

🗓 2026-03-23 · 13:10 (GMT+2)

🔹 Request

User requested category and subcategory subrows on the workout list view, and asked for the workout detail category presentation to use more native SwiftUI elements.

🔧 Changes Made
	•	Updated `WorkoutRowView` so categories and subcategories now render on separate metadata lines in the workout list.
	•	Reworked the workout-detail category display in `WorkoutDetailView` away from custom tiles and toward simpler native row-style content with standard symbols and dividers.

Files modified:
	•	TrainState/Views/Components/WorkoutRowView.swift
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Workout list rows now show clearer category hierarchy with subrows, and workout detail uses a much more restrained native-feeling category presentation.

⸻

🗓 2026-03-23 · 13:02 (GMT+2)

🔹 Request

User reported that the recent category styling looked bad on the workout list and workout detail screens.

🔧 Changes Made
	•	Removed the chip-heavy category treatment from `WorkoutRowView` and simplified list rows back to a cleaner single metadata line with a small tag icon.
	•	Restyled the workout-detail category display in `WorkoutDetailView` into flatter labeled tiles with subtle color accents instead of bulky capsule-style rows.

Files modified:
	•	TrainState/Views/Components/WorkoutRowView.swift
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Categories now read much more cleanly in the workout list, and the workout detail screen keeps category visibility without the heavy badge styling.

⸻

🗓 2026-03-23 · 12:51 (GMT+2)

🔹 Request

User requested that the category chips on the workout detail screen should look better.

🔧 Changes Made
	•	Reworked the category and subcategory display in `WorkoutDetailView` away from the previous generic capsule grid.
	•	Updated the detail-screen classification UI to use cleaner rounded rows with real category colors and distinct folder/tag icons for categories and subcategories.
	•	Kept categories and subcategories visually separated while making the styling feel more native and easier to scan.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

The workout detail screen now presents categories and subcategories in a much cleaner, more legible style that better matches the rest of the app.

⸻

🗓 2026-03-23 · 12:45 (GMT+2)

🔹 Request

User requested that set-row swipe directions be adjusted so duplicate is on the left and delete is on the right, and reported that the category selector sheet still lagged when swiping down.

🔧 Changes Made
	•	Updated `ExerciseEditorSheetView` so `Duplicate` is now a leading swipe action and `Delete` remains the trailing destructive action.
	•	Removed category-assignment persistence from the category sheet’s direct `onDismiss` path in both `WorkoutDetailView` and `WorkoutListView`.
	•	Deferred category assignment saves until shortly after the sheet closes so the swipe-down dismissal animation can complete first.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Set-row swipe directions now match the requested behavior, and category selector dismissal should feel smoother because category saves no longer run directly on the interactive close gesture.

⸻

🗓 2026-03-23 · 12:37 (GMT+2)

🔹 Request

User requested that the category and subcategory selectors should feel much nicer and use more native SwiftUI, and that categories and subcategories should display in the workout list view.

🔧 Changes Made
	•	Rebuilt `CategoryAndSubcategorySelectionView` around a native grouped `List` with tap-to-select rows, checkmarks, and standard `Form` sheets for adding categories and subcategories.
	•	Removed the older gradient and glass-heavy selector presentation in favor of a cleaner system-style layout.
	•	Updated `WorkoutRowView` so linked categories and subcategories show in the workout list as colored chips instead of plain subtitle text.

Files modified:
	•	TrainState/Views/CategorySelection/CategoryAndSubcategorySelectionView.swift
	•	TrainState/Views/Components/WorkoutRowView.swift

✅ Result

Category selection now feels more native and easier to scan, and workout rows in the list make attached categories and subcategories much more visible at a glance.

⸻

🗓 2026-03-23 · 12:24 (GMT+2)

🔹 Request

User requested that the edit-exercise sets should look nicer, use a more native list style, and support swipe options.

🔧 Changes Made
	•	Updated `ExerciseEditorSheetView` to use a native `List`-based sheet layout so set rows participate in real list-row interactions.
	•	Added trailing swipe actions to duplicate or delete an individual set row, with delete presented as a destructive red action.
	•	Added matching long-press context menu actions for duplicate and delete on each set row.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

Set editing now feels closer to native iOS list editing, with clearer row structure and faster set actions available directly from swipe gestures.

⸻

🗓 2026-03-23 · 12:18 (GMT+2)

🔹 Request

User identified repeated `WorkoutRoute` decode logging during sheet interactions, suggesting route decoding was contributing to the UI hitching.

🔧 Changes Made
	•	Removed verbose route encode/decode logging from `WorkoutRoute`.
	•	Added transient in-memory caching for decoded route arrays so the same archived route data is not repeatedly unarchived on every property read.
	•	Added a lightweight route-data fingerprint so cached routes are reused while still invalidating correctly when the underlying route data changes.

Files modified:
	•	TrainState/Models/WorkoutRoute.swift

✅ Result

Route-backed workouts now avoid repeated large route decodes during view updates, reducing main-thread work and eliminating the route debug log spam that was surfacing during sheet interactions.

⸻

🗓 2026-03-23 · 12:08 (GMT+2)

🔹 Request

User requested that the last exercise-editor sheet structure change be reverted after confirming the lag affects all sheets, not just that screen.

🔧 Changes Made
	•	Reverted the inline-header sheet structure in `ExerciseEditorSheetView`.
	•	Restored the previous `NavigationStack` and toolbar-based header for the exercise editor.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

The exercise editor is back on its prior navigation structure, avoiding a local UI change that did not address the broader sheet lag issue.

⸻

🗓 2026-03-23 · 12:03 (GMT+2)

🔹 Request

User reported that the exercise editor still froze partway through the swipe-down dismissal.

🔧 Changes Made
	•	Removed the inner `NavigationStack` and toolbar-based navigation chrome from `ExerciseEditorSheetView`.
	•	Replaced that navigation container with a lightweight inline header bar so the sheet dismiss gesture animates a simpler view hierarchy.
	•	Kept the existing close and keyboard-dismiss actions without routing them through navigation toolbar presentation.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

The exercise editor now dismisses from a simpler sheet structure, reducing the amount of navigation UI SwiftUI has to animate during the interactive swipe-down gesture.

⸻

🗓 2026-03-23 · 11:57 (GMT+2)

🔹 Request

User reported that the exercise editor still lagged on swipe-down dismissal even when no changes were being saved, suggesting the animation path itself was heavy.

🔧 Changes Made
	•	Replaced the `Form`-based layout in `ExerciseEditorSheetView` with a lighter `ScrollView` and card-based stack.
	•	Kept the same editing controls, set-plan actions, and swipe-to-delete behavior while removing the heavier form/table container from the interactive sheet.
	•	Added interactive keyboard dismissal on the scroll layout so the sheet and keyboard gestures cooperate more cleanly.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

The exercise editor sheet now presents a lighter view hierarchy, which should reduce hitching during the swipe-down dismissal animation even when no persistence work is happening.

⸻

🗓 2026-03-23 · 11:49 (GMT+2)

🔹 Request

User reported that categories were no longer visible or manageable and requested the ability to add categories again.

🔧 Changes Made
	•	Updated `WorkoutDetailView` so the `Categories` section always appears, even when the workout has no linked categories yet.
	•	Added an inline `Add Categories` or `Edit Categories` action on the workout detail screen.
	•	Reused `CategoryAndSubcategorySelectionView` from workout detail and applied the selected categories and subcategories back onto the workout when the sheet closes.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Categories can now be added and edited directly from workout detail, and the section no longer disappears when nothing is assigned yet.

⸻

🗓 2026-03-23 · 11:44 (GMT+2)

🔹 Request

User reported that swiping down to dismiss the exercise editor was still laggy.

🔧 Changes Made
	•	Removed the post-dismiss save behavior from `WorkoutDetailView` so closing the exercise editor no longer triggers persistence work on the dismissal path.
	•	Changed workout-detail exercise editing to debounce persistence while the sheet is open, saving only after a short idle period instead of when the swipe-down gesture completes.
	•	Kept the in-memory workout exercise synchronized immediately so the UI still reflects edits without waiting for the sheet to close.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Swiping down to dismiss the exercise editor is now lighter because the close gesture no longer has to wait on save work tied to the sheet dismissal.

⸻

🗓 2026-03-23 · 11:35 (GMT+2)

🔹 Request

User reported that workout categories were not visible and requested that they show both on the outside workout rows and on the workout detail screen.

🔧 Changes Made
	•	Updated `WorkoutRowView` to render a compact linked classification summary beneath the workout title when categories or subcategories are attached.
	•	Added a dedicated `Categories` section to `WorkoutDetailView` that shows linked categories and subcategories as tinted chips.
	•	Kept the display tied to the workout’s stored category relationships instead of reintroducing the old broad workout-type label path.

Files modified:
	•	TrainState/Views/Components/WorkoutRowView.swift
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Linked workout categories are now visible from the main workout list and inside workout detail, making the saved classification obvious in both contexts.

⸻

🗓 2026-03-23 · 11:27 (GMT+2)

🔹 Request

User requested that when an exercise is added, the workout should automatically add the linked category as well.

🔧 Changes Made
	•	Updated the workout-detail exercise add flow in `WorkoutDetailView` to automatically sync the selected exercise’s subcategory onto the workout.
	•	Also auto-linked the parent category of that subcategory onto the workout when it was not already attached.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Adding an exercise from workout detail now automatically keeps the workout’s category and subcategory relationships in sync with the exercise that was added.

⸻

🗓 2026-03-23 · 11:21 (GMT+2)

🔹 Request

User reported that swiping down to close the edit exercise sheet felt laggy.

🔧 Changes Made
	•	Removed the synchronous persistence work from the exercise-editor sheet’s direct dismiss callback in `WorkoutDetailView`.
	•	Moved exercise persistence to the next runloop tick after the sheet closes so the interactive swipe-down dismissal can complete first.
	•	Added exercise draft change detection so the detail-screen editor now skips the save entirely when nothing actually changed.
	•	Deferred the post-dismiss save slightly further so it starts after the close animation has already progressed.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Closing the exercise editor by swiping down now feels smoother because the workout-exercise save work no longer blocks the dismissal gesture itself.

⸻

🗓 2026-03-23 · 11:13 (GMT+2)

🔹 Request

User requested the ability to swipe and delete an individual set row while editing an exercise.

🔧 Changes Made
	•	Added trailing swipe-to-delete actions to individual set rows in `ExerciseEditorSheetView`.
	•	Hooked set-row deletion into the existing set-plan state sync so set counts and legacy summary values update correctly after a delete.
	•	Removed the bulk `Clear Plan` action so set removal now happens through direct row deletion instead of a full-plan destructive control.
	•	Removed the duplicate top-bar close action from the exercise editor so the sheet only shows one close button.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

Individual sets can now be removed directly with a swipe gesture inside the exercise editor instead of clearing the whole set plan.

⸻

🗓 2026-03-23 · 11:08 (GMT+2)

🔹 Request

User requested that the edit exercise sheet should open in full view by default.

🔧 Changes Made
	•	Updated `ExerciseEditorSheetView` to present with the large detent only, so the sheet now opens at full height by default everywhere it is used.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

The edit exercise sheet now opens in full view by default instead of starting in the medium-height presentation.

⸻

🗓 2026-03-23 · 11:03 (GMT+2)

🔹 Request

User requested that the set plan should be easier to add.

🔧 Changes Made
	•	Replaced the set-count modal interaction in `ExerciseEditorSheetView` with inline minus/plus controls so users can change set count directly in the form.
	•	Added quick-start set-count buttons (`3 Sets`, `4 Sets`, `5 Sets`) when no set plan exists yet.
	•	Added an inline `Add Set` action inside the set-plan section so users can build a plan incrementally without leaving the sheet.
	•	Added a `Clear Plan` action and kept the existing per-set rep/weight editing controls for fine tuning.

Files modified:
	•	TrainState/Views/Components/ExerciseEditorSheetView.swift

✅ Result

Set plans are now faster to create because users can start and expand them directly inside the sheet instead of relying on a separate sets picker flow first.

⸻

🗓 2026-03-23 · 10:52 (GMT+2)

🔹 Request

User requested that workout-detail exercise rows should open the add-sets screen on tap, while a long press should offer both `Add Sets` and navigation to the exercise page.

🔧 Changes Made
	•	Changed workout-detail exercise rows from direct navigation links into tap targets that open `ExerciseEditorSheetView`.
	•	Added per-row long-press context menus with `Add Sets` and `View Exercise Page` actions.
	•	Added local draft/persistence plumbing in `WorkoutDetailView` so set edits from the sheet write back to the underlying `WorkoutExercise`.
	•	Reused the existing set-note parsing and serialization pattern so completed/planned sets round-trip correctly from the detail screen.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Tapping an exercise in workout detail now opens the add-sets editor, while long pressing the row gives explicit options to either add sets or navigate to the exercise insights page.

⸻

🗓 2026-03-23 · 10:39 (GMT+2)

🔹 Request

User reported that several screens, including exercises and settings, were showing double navigation arrows.

🔧 Changes Made
	•	Removed the built-in chevron from the shared `SettingsRow` component so `NavigationLink` rows in settings only show the system navigation indicator once.
	•	Removed manual chevrons from workout-detail exercise rows that were already wrapped in `NavigationLink`.
	•	Removed manual chevrons from navigable cards in `SubcategoryLastLoggedView`.
	•	Removed the extra chevron from the analytics strength inventory card, which was already presented inside a `NavigationLink`.

Files modified:
	•	TrainState/Views/SettingsView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/SubcategoryLastLoggedView.swift
	•	TrainState/Views/AnalyticsView.swift

✅ Result

Rows and cards that navigate now show a single navigation arrow instead of drawing both a manual chevron and the system `NavigationLink` indicator.

⸻

🗓 2026-03-23 · 10:28 (GMT+2)

🔹 Request

User requested an `Add Exercises` button on the workout detail screen so exercises can be added without going into edit mode.

🔧 Changes Made
	•	Added an inline `Add Exercises` action to `WorkoutDetailView` inside the exercises section.
	•	Hooked the detail screen up to the existing `UnifiedExercisePickerView`, subcategory queries, and exercise-template queries so it reuses the same exercise library flow as add/edit screens.
	•	Filtered available exercise subcategories by the workout’s Apple workout activity before showing the picker.
	•	Persisted newly added exercises directly onto the current workout, preserving order and reusing the most recent matching exercise values when available.
	•	Kept the detail-screen add flow inline so adding an exercise does not automatically open exercise editing afterward.

Files modified:
	•	TrainState/Views/WorkoutDetailView.swift

✅ Result

Users can now add exercises directly from the workout detail screen without entering the full edit flow.

⸻

🗓 2026-03-23 · 10:18 (GMT+2)

🔹 Request

User requested that when a workout filter is applied and there are no matching workouts, the empty state should say no specific workouts instead of the generic no-workouts message.

🔧 Changes Made
	•	Updated the workout-list empty state in `WorkoutListView` to derive its title from the active filter.
	•	Updated the empty-state icon to use the active workout filter’s Apple workout icon instead of always showing the generic running icon.
	•	Kept the default empty-state copy for the unfiltered list and added filter-specific empty-state messaging when a workout-type filter is active.

Files modified:
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Filtered empty states now say things like `No Running Workouts` instead of always showing the generic `No Workouts` message.

⸻

🗓 2026-03-23 · 10:11 (GMT+2)

🔹 Request

User reported that the new `Import All` HealthKit option was very laggy.

🔧 Changes Made
	•	Added a batched HealthKit import path in `HealthKitRecentWorkoutImporter` that inserts all selected workouts and saves once instead of saving after every imported workout.
	•	Updated the bulk-import action in `WorkoutListView` to use that batched importer path.
	•	The bulk path now skips the expensive per-workout route fetch used by single-item imports, which removes the main source of UI lag during large imports.

Files modified:
	•	TrainState/Services/HealthKitRecentWorkoutImporter.swift
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Bulk HealthKit import now runs much more smoothly because it avoids repeated route downloads and repeated SwiftData saves while importing many workouts in one go.

⸻

🗓 2026-03-23 · 10:02 (GMT+2)

🔹 Request

User requested a button that imports all HealthKit workouts that have not yet been imported.

🔧 Changes Made
	•	Added an `Import All` action to the HealthKit menu in `WorkoutListView`.
	•	Computed the list of recent HealthKit workouts whose `hkUUID` is not already linked in the app and used that as the bulk-import source.
	•	Implemented a bulk import routine that imports each available workout through the existing HealthKit importer and marks successful imports so the menu updates immediately.
	•	Added partial-failure reporting so users still get feedback if some workouts fail during the bulk import run.

Files modified:
	•	TrainState/Views/WorkoutListView.swift

✅ Result

Users can now import every recent unlinked HealthKit workout in one action instead of tapping each workout individually.

⸻

🗓 2026-03-23 · 09:41 (GMT+2)

🔹 Request

User requested a full migration of the workout-type refactor so the app no longer keeps relying on the old broad workout bucket logic in remaining screens and stored data paths.

🔧 Changes Made
	•	Added workout-data migration to backfill missing Apple workout activities onto existing workouts during app initialization.
	•	Updated workout list filtering to use exact Apple workout activities instead of the old broad `WorkoutFilter` bucket enum, and sorted the Apple activity filter list for usability.
	•	Updated analytics filtering to use Apple workout activities through a menu-based filter instead of the old `All` / `Strength` segmented control.
	•	Refactored developer seed data to resolve categories and subcategories by Apple workout activity and to save seeded workouts with Apple workout activity metadata.
	•	Replaced remaining visible broad workout labels in calendar, settings backup preview, duplicate-workout warnings, and subcategory history with the workout’s Apple workout display name.

Files modified:
	•	TrainState/Models/Workout.swift
	•	TrainState/Managers/DataInitializationManager.swift
	•	TrainState/Views/WorkoutListView.swift
	•	TrainState/Views/AnalyticsView.swift
	•	TrainState/Views/DeveloperOptionsView.swift
	•	TrainState/Views/CalendarView.swift
	•	TrainState/Views/SettingsView.swift
	•	TrainState/Views/SubcategoryLastLoggedView.swift
	•	TrainState/Views/AddWorkoutView.swift

✅ Result

The app now uses Apple workout activities as the active workout-type model across persistence backfill, list filtering, analytics filtering, seeded data, and the remaining user-facing labels that previously still showed the old broad workout buckets.

⸻

🗓 2026-03-23 · 09:18 (GMT+2)

🔹 Request

User asked whether the workout-type migration was fully completed everywhere and requested a full migration to the new Apple workout type model.

🔧 Changes Made
	•	Restored `WorkoutCategory.workoutType` as an explicit compatibility field while keeping Apple workout activity as the new primary linkage, so persisted legacy data can still be migrated safely.
	•	Added automatic startup migration in `DataInitializationManager` to backfill missing Apple workout activity values for existing categories and strength templates.
	•	Extended `StrengthWorkoutTemplate` persistence to store Apple workout activities directly, updated backup export/import to preserve that field, and switched template save/apply flows to use the exact Apple workout activity.
	•	Updated strength-template management to filter subcategories and display template metadata using the Apple workout activity rather than only the broad workout bucket.

Files modified:
	•	TrainState/Models/WorkoutCategory.swift
	•	TrainState/Models/StrengthWorkoutTemplate.swift
	•	TrainState/Models/Workout.swift
	•	TrainState/Managers/CloudKitManager.swift
	•	TrainState/Managers/DataInitializationManager.swift
	•	TrainState/Views/AddWorkoutView.swift
	•	TrainState/Views/WorkoutListView.swift
	•	TrainState/Views/WorkoutDetailView.swift
	•	TrainState/Views/StrengthTemplatesManagementView.swift

✅ Result

The migration now covers persisted categories and strength templates instead of only UI interpretation, so existing data is backfilled to Apple workout activities and newly saved template/category data follows the new workout-type model end to end.
