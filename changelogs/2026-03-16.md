🗓 2026-03-16 · 15:49 (GMT+2)

🔹 Request

User requested HealthKit recent workouts to show workout titles as workout name, then time, then day on separate lines.

🔧 Changes Made
• Kept the existing top-level HealthKit recent workouts menu layout in place.
• Increased the HealthKit recent workout fetch window from 10 items to 30 items.
• Reformatted each recent HealthKit workout title into a three-line label showing the actual Apple workout name, workout time, and relative day.
• Added a leading HealthKit activity-specific icon to each recent workout row so the symbol matches the actual Apple workout shown in the title.
• Allowed recent workout title and subtitle text to wrap freely instead of being constrained by the default compact menu text layout.
• Simplified the secondary workout subtitle so it now only shows supporting metadata like distance and source without repeating the date and time.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

The HealthKit menu keeps its previous layout, now shows the last 30 workouts, and each workout option is easier to scan with a workout-type icon plus a stacked title showing the actual Apple workout name, time, and day.

⸻

🗓 2026-03-17 · 00:04 (GMT+2)

🔹 Request

User asked whether HealthKit runs should import as `Outdoor Running` when HealthKit marks them as outdoor.

🔧 Changes Made
• Extended `HealthKitRecentWorkoutMenuItem` to carry HealthKit indoor/outdoor metadata alongside the Apple workout activity.
• Updated HealthKit import/attach flows to persist that location metadata onto workouts.
• Updated the recent HealthKit workouts list title rendering to use Apple activity + location, so runs can appear as `Outdoor Running` or `Indoor Running` when HealthKit provides that information.

Files modified:
• TrainState/Services/HealthKitRecentWorkoutImporter.swift
• TrainState/Views/WorkoutListView.swift

✅ Result

Recent HealthKit import candidates and imported workouts can now distinguish indoor vs. outdoor runs from HealthKit metadata instead of flattening them to just `Running`.

⸻

🗓 2026-03-16 · 16:43 (GMT+2)

🔹 Request

User requested that imported Apple workouts should no longer explicitly set or assign the app’s separate workout type.

🔧 Changes Made
• Updated `Workout.type` to derive from the stored Apple workout activity whenever a HealthKit activity is present.
• Removed the explicit app workout-type assignment from `HealthKitRecentWorkoutImporter` so imports only persist Apple workout metadata and metrics.

Files modified:
• TrainState/Models/Workout.swift
• TrainState/Services/HealthKitRecentWorkoutImporter.swift

✅ Result

Imported Apple workouts no longer persist a second app-specific workout type during import; the app derives any broad type view from the Apple workout activity on demand.

⸻

🗓 2026-03-16 · 16:34 (GMT+2)

🔹 Request

User said they no longer want to use custom workout categories and want Apple workout categories to be the only visible workout classification.

🔧 Changes Made
• Removed custom workout category/subcategory assignment UI from workout creation, editing, detail, and workout rows.
• Stopped attaching workout categories/subcategories to newly created and edited workouts, so Apple workout classification is now the only workout categorization surfaced in the main flow.
• Kept the deeper category/subcategory data model in place for exercise-template internals to avoid a broader schema break during this pass.

Files modified:
• TrainState/Views/AddWorkoutView.swift
• TrainState/Views/EditWorkoutView.swift
• TrainState/Views/WorkoutDetailView.swift
• TrainState/Views/Components/WorkoutRowView.swift

✅ Result

The workout logging and viewing flow now treats Apple workout classification as the only visible workout category system, while legacy custom category infrastructure is no longer part of the main workout UX.

⸻

🗓 2026-03-16 · 16:18 (GMT+2)

🔹 Request

User reported that Apple workout types were still being forced through poor bucket mapping, especially with too many activities under `Cardio`, and called out indoor/outdoor handling as incorrect.

🔧 Changes Made
• Changed the create/edit flow so the exact Apple workout activity is now the primary selector, instead of filtering Apple activities through the app’s broad workout buckets.
• Added separate workout location storage and UI for activities where HealthKit treats indoor/outdoor as location metadata rather than a different activity type.
• Updated workout row/detail labels to show the exact Apple activity name with location context only when applicable.

Files modified:
• TrainState/Models/Workout.swift
• TrainState/Managers/CloudKitManager.swift
• TrainState/Extensions/HKWorkoutActivityType+WorkoutType.swift
• TrainState/Views/AddWorkoutView.swift
• TrainState/Views/EditWorkoutView.swift
• TrainState/Views/WorkoutDetailView.swift
• TrainState/Views/Components/WorkoutRowView.swift

✅ Result

Manual workouts now store a specific Apple workout activity directly, app buckets are derived from that activity instead of constraining it, and indoor/outdoor is treated as location metadata instead of pretending to be separate workout types.

⸻

🗓 2026-03-16 · 16:10 (GMT+2)

🔹 Request

User reported that the route modal pickers were overlapping each other.

🔧 Changes Made
• Moved the route modal `Map Style` segmented picker out of the navigation bar toolbar and into the sheet content above the map.
• Kept the existing `Done` toolbar action so the modal controls no longer compete for the same navigation bar space.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

The route modal controls no longer overlap, and the map-style picker now has enough horizontal space to render cleanly above the map.

⸻

🗓 2026-03-16 · 16:03 (GMT+2)

🔹 Request

User asked that manual workout creation should also support every Apple workout type, not just HealthKit imports.

🔧 Changes Made
• Reused the existing `hkActivityTypeRaw` storage as a general Apple workout activity field so manually created and edited workouts can persist a specific Apple activity.
• Added exact Apple workout type pickers to `AddWorkoutView` and `EditWorkoutView`, filtered to the selected app workout bucket while still covering the full Apple activity list.
• Surfaced the saved Apple workout activity name in workout rows and the workout detail header when it is more specific than the app bucket label.

Files modified:
• TrainState/Models/Workout.swift
• TrainState/Extensions/HKWorkoutActivityType+WorkoutType.swift
• TrainState/Views/AddWorkoutView.swift
• TrainState/Views/EditWorkoutView.swift
• TrainState/Views/WorkoutDetailView.swift
• TrainState/Views/Components/WorkoutRowView.swift

✅ Result

Users can now choose and save the exact Apple workout activity when creating or editing a workout, while the app still keeps its broader workout buckets for filtering, categories, and existing logic.

⸻

🗓 2026-03-16 · 15:48 (GMT+2)

🔹 Request

User requested that every Apple workout type should be mapped in the app.

🔧 Changes Made
• Added a shared `HKWorkoutActivityType` mapping extension that explicitly covers the full current Apple workout activity list from the installed SDK.
• Routed both HealthKit import flows through the shared mapper so recent-workout previews and imported workouts classify Apple activities consistently.
• Added regression coverage for representative Apple workout activities across each app workout bucket.

Files modified:
• TrainState/Extensions/HKWorkoutActivityType+WorkoutType.swift
• TrainState/Services/HealthKitRecentWorkoutImporter.swift
• TrainState/Views/WorkoutListView.swift
• TrainStateTests/HealthKitRecentWorkoutImporterTests.swift

✅ Result

HealthKit workouts from Apple’s full current workout activity set now map consistently into the app’s workout categories instead of relying on a partial subset that left many activity types falling through to `Other`.

⸻

🗓 2026-03-16 · 16:03 (GMT+2)

🔹 Request

User requested an option in the workout route modal to switch the map into satellite view.

🔧 Changes Made
• Added a reusable `RouteMapDisplayMode` type to `RouteMapView` and wired the underlying `MKMapView` to update its `mapType`.
• Added a segmented `Map Style` control to the workout-list route preview modal so users can switch between `Standard` and `Satellite`.
• Reset the route modal map style back to `Standard` when dismissing the sheet.

Files modified:
• TrainState/Views/Components/RouteMapView.swift
• TrainState/Views/WorkoutListView.swift

✅ Result

The workout route modal now lets users switch to satellite imagery while viewing route data, improving map readability for outdoor workouts.

⸻

🗓 2026-03-16 · 16:02 (GMT+2)

🔹 Request

User clarified that `Open in Calendar` should launch the iOS Calendar app rather than navigate to the app’s internal Calendar tab.

🔧 Changes Made
• Replaced the in-app calendar tab handoff in `WorkoutListView` with an `openURL` deep link to the system Calendar app using the workout’s date.
• Removed the temporary shared `AppStorage` state that had been added to coordinate tab selection and calendar focus.

Files modified:
• TrainState/Views/WorkoutListView.swift
• TrainState/Views/MainTabView.swift
• TrainState/Views/CalendarView.swift

✅ Result

Choosing `Open in Calendar` from a workout row now opens the user’s iOS Calendar app at the workout date instead of switching to TrainState’s internal Calendar tab.

⸻

🗓 2026-03-16 · 16:01 (GMT+2)

🔹 Request

User requested additional workout-row long-press actions for assigning categories, viewing exercises, and opening the workout in Calendar.

🔧 Changes Made
• Added `Assign Categories` to the workout-row context menu and wired it to the existing `CategoryAndSubcategorySelectionView`, persisting changes back to the selected workout on dismiss.
• Added `View Exercises` to the context menu for workouts that contain exercises, presented as a dedicated sheet using the existing `ExerciseCardView`.
• Added a lightweight shared tab/calendar handoff using `AppStorage`, allowing `Open in Calendar` from `WorkoutListView` to switch to the Calendar tab and focus the workout’s week.

Files modified:
• TrainState/Views/WorkoutListView.swift
• TrainState/Views/CalendarView.swift
• TrainState/Views/MainTabView.swift

✅ Result

Workout rows now support direct category assignment, quick exercise review, and a one-tap jump into the Calendar tab for the relevant workout week, reducing the need to open the workout detail screen first.

⸻

🗓 2026-03-16 · 15:57 (GMT+2)

🔹 Request

User requested more useful long-press options on workout rows in `WorkoutListView`.

🔧 Changes Made
• Expanded the workout-row context menu with `Save as Template` for strength workouts that already contain exercises.
• Added a `View Route` long-press action for workouts that include route data, presented in a dedicated route sheet from the list.
• Added the supporting template-save alert flow in `WorkoutListView`, including default unique template naming and direct template persistence.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

Long-pressing a workout row now exposes more useful shortcuts, letting users save eligible strength sessions as templates and open route data directly from the list without first entering the workout detail screen.

⸻

🗓 2026-03-16 · 15:45 (GMT+2)

🔹 Request

User reported a SwiftUI compiler type-check timeout in `WorkoutListView.swift` after the workout-row interaction updates.

🔧 Changes Made
• Broke the `WorkoutListView` `body` into smaller computed views and helper builders for list content, toolbar content, workout rows, and HealthKit menu sections.
• Extracted the HealthKit candidate tap flow and the import alert binding into dedicated helpers to reduce the size of the main view expression tree.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

`WorkoutListView` now presents the same UI behavior through smaller SwiftUI subexpressions, which should unblock the compiler from timing out while type-checking the main `body`.

⸻

🗓 2026-03-16 · 15:40 (GMT+2)

🔹 Request

User reported that workout rows in `WorkoutListView` were showing two arrows and requested long-press options on those items.

🔧 Changes Made
• Updated `WorkoutRowView` with a `showsChevron` toggle so the list can suppress the custom chevron when `NavigationLink` is already providing one.
• Changed `WorkoutListView` rows to use the chevron-free row variant, removing the duplicate accessory arrow from the workout list.
• Added a long-press context menu on workout rows with `Edit Workout` and `Delete Workout` actions.
• Added list-level edit sheet and delete confirmation handling so long-press actions reuse the existing workout editing flow and support direct deletion from the list.

Files modified:
• TrainState/Views/WorkoutListView.swift
• TrainState/Views/Components/WorkoutRowView.swift

✅ Result

Workout rows now show a single arrow in the list, and users can long-press any workout to edit or delete it directly without opening the detail screen first.

⸻

🗓 2026-03-16 · 15:39 (GMT+2)

🔹 Request

User reported that restoring purchases gave no feedback and premium features only appeared after force-closing and reopening the app.

🔧 Changes Made
• Updated `PurchaseManager` to subscribe to RevenueCat customer info updates so restored entitlements are applied immediately while the app is still running.
• Added explicit restore state handling to prevent overlapping restore attempts and to mark the premium check complete as soon as restore/customer info returns.
• Added a `Restore Purchases` action with loading, success, and error feedback in `SubscriptionInfoView`, including immediate entitlement refresh after restore.

Files modified:
• TrainState/Managers/PurchaseManager.swift
• TrainState/Views/SubscriptionInfoView.swift

✅ Result

Restoring purchases now gives visible progress and completion feedback, and premium-gated UI can unlock immediately without requiring the app to be closed and reopened.

⸻

🗓 2026-03-16 · 14:20 (GMT+2)

🔹 Request

User reported an Xcode preview/build failure caused by an extraneous top-level `}` in `AnalyticsView.swift`.

🔧 Changes Made
• Removed the stray closing brace after the `#Preview` block in `AnalyticsView.swift`.

Files modified:
• TrainState/Views/AnalyticsView.swift

✅ Result

The syntax error blocking previews and scheme builds is removed, so `TrainState` can compile past `AnalyticsView.swift` again.

⸻

🗓 2026-03-16 · 14:07 (GMT+2)

🔹 Request

User reported that placing the CTA after the form still left it permanently at the bottom of the screen rather than underneath the form content.

🔧 Changes Made
• Moved the `AddWorkoutView` CTA into a footer-only trailing `Section` inside the `Form`, so it participates in the form’s scroll flow.
• Kept the footer row visually cleared to avoid the standard form cell chrome while placing the action after the rest of the form content.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Save Workout` / `Start Live Session` CTA now sits underneath the form content in the scrolling layout instead of being permanently anchored to the bottom of the screen.

⸻

🗓 2026-03-16 · 14:05 (GMT+2)

🔹 Request

User requested that the save/live-session CTA should appear at the end of the form, not as a pinned bottom action above it.

🔧 Changes Made
• Replaced the `AddWorkoutView` bottom `safeAreaInset` layout with a `VStack` that places the CTA directly after the `Form`.
• Kept the standalone button styling, while changing its placement so it reads as the final screen action rather than a floating/pinned control.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Save Workout` / `Start Live Session` CTA now renders after the form content instead of sitting above it as a pinned inset.

⸻

🗓 2026-03-16 · 14:03 (GMT+2)

🔹 Request

User requested that the pinned save/live-session CTA should sit at the very bottom instead of floating above it.

🔧 Changes Made
• Removed the extra bottom padding from the `AddWorkoutView` bottom `safeAreaInset` that hosts the CTA.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Save Workout` / `Start Live Session` CTA now sits directly against the bottom safe area instead of appearing elevated above it.

⸻

🗓 2026-03-16 · 14:02 (GMT+2)

🔹 Request

User reported that the save/live-session CTA was still rendering inside a form-style container.

🔧 Changes Made
• Removed the save/live-session CTA from the `Form` rows in `AddWorkoutView`.
• Repositioned the button into a bottom `safeAreaInset`, so it renders as a standalone pinned action instead of inheriting form cell chrome.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Save Workout` / `Start Live Session` CTA now renders outside the form container, matching the standalone action presentation more closely.

⸻

🗓 2026-03-16 · 14:00 (GMT+2)

🔹 Request

User requested that the save/live-session CTA should render like the `Edit Workout` button exactly.

🔧 Changes Made
• Updated the `AddWorkoutView` CTA to use the same iOS 26 `glassProminent` button style and label spacing as `WorkoutDetailView`’s `Edit Workout` button.
• Removed the custom iOS 26 glass-card workaround so the save/live-session action now follows the same rendering path as the preferred reference button.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Save Workout` / `Start Live Session` CTA now renders using the same style path as the `Edit Workout` button instead of a custom approximation.

⸻

🗓 2026-03-16 · 13:58 (GMT+2)

🔹 Request

User reported that the workout save button was still rendering with an oversized double-container appearance.

🔧 Changes Made
• Cleared the `Form` row background and separator styling around the `AddWorkoutView` save button row.
• Kept the button’s own styling intact so only the intended CTA shape renders, without the extra rounded form cell behind it.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The save/live-session CTA no longer shows the doubled oversized pill effect caused by the form row container.

⸻

🗓 2026-03-16 · 13:55 (GMT+2)

🔹 Request

User reported that the `Start Live Session` button had an unwanted border around it.

🔧 Changes Made
• Replaced the `AddWorkoutView` save/live-session CTA’s `glassProminent` button style with the app’s custom `glassCard` treatment on iOS 26+.
• Kept the button sizing and action behavior intact while removing the system-provided outline that was causing the visual issue.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Start Live Session` button now uses the app’s glass styling without the extra border that appeared with the system glass button style.

⸻

🗓 2026-03-16 · 13:53 (GMT+2)

🔹 Request

User requested that the updated iCloud backup action buttons should be a bit smaller.

🔧 Changes Made
• Reduced the shared backup action-button vertical padding in `SettingsView` to shrink the overall button height.
• Tightened the older-iOS rounded fallback corner radius slightly so the smaller size still feels proportionate.

Files modified:
• TrainState/Views/SettingsView.swift

✅ Result

The `Backup to iCloud` and `Refresh Backups` buttons now keep the preferred style but with a more compact footprint.

⸻

🗓 2026-03-16 · 13:52 (GMT+2)

🔹 Request

User requested that settings actions such as `Backup to iCloud` and `Refresh Backups` should also stop using the new bordered button style.

🔧 Changes Made
• Replaced the iCloud backup section’s bordered button styles in `SettingsView` with a shared action-button helper that matches the preferred `Edit Workout` visual treatment.
• Applied `glassProminent` on iOS 26+ and the same rounded filled fallback on earlier iOS versions for both backup actions.

Files modified:
• TrainState/Views/SettingsView.swift

✅ Result

The `Backup to iCloud` and `Refresh Backups` buttons now match the preferred action-button style instead of the newer bordered appearance.

⸻

🗓 2026-03-16 · 13:51 (GMT+2)

🔹 Request

User requested that the new bordered workout action buttons, such as `Start Live Session`, should match the `Edit Workout` button style instead.

🔧 Changes Made
• Updated the main workout creation CTA in `AddWorkoutView` to stop using the new bordered system styles.
• Switched the button to the same visual treatment as `Edit Workout`: `glassProminent` on iOS 26+ with the same rounded filled fallback on earlier iOS versions.

Files modified:
• TrainState/Views/AddWorkoutView.swift

✅ Result

The `Start Live Session` / `Save Workout` action now matches the `Edit Workout` button style instead of the newer bordered appearance.

⸻

🗓 2026-03-16 · 13:48 (GMT+2)

🔹 Request

User requested that HealthKit import should only show a popup when import or attach fails, not on success.

🔧 Changes Made
• Removed the HealthKit success-message alert state from `WorkoutListView`, so successful imports and attaches complete silently.
• Narrowed the HealthKit alert presentation logic to error cases only, preserving failure popups while removing success and empty-refresh popups.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

HealthKit imports and attachments now complete without a success popup, while failures still surface an alert to explain what went wrong.

⸻

🗓 2026-03-16 · 13:49 (GMT+2)

🔹 Request

User requested that recent HealthKit workouts should refresh automatically when opening the workouts screen.

🔧 Changes Made
• Updated `WorkoutListView` to trigger a HealthKit recent-workouts refresh during `onAppear`, after first loading the cached workout list for immediate display.
• Added a silent refresh path so automatic loading does not show empty-state or error alerts on screen open, while the manual refresh action keeps its existing feedback.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

The workouts screen now refreshes recent HealthKit imports automatically when opened, while still showing cached results immediately and avoiding noisy alerts during background refresh.

⸻

🗓 2026-03-16 · 13:45 (GMT+2)

🔹 Request

User reported a SwiftUI compiler error in `WorkoutListView` where `colorScheme` was being treated as the wrong type.

🔧 Changes Made
• Added the missing `@Environment(\.colorScheme)` binding to `WorkoutListView`, so the banner metric pill background resolves against the actual current `ColorScheme`.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

The `metricPill` background opacity check now compiles correctly because `colorScheme` resolves to the environment value instead of the SwiftUI modifier overload.

⸻

🗓 2026-03-16 · 13:42 (GMT+2)

🔹 Request

User reported that the Live Activity does not update until opening the app again.

🔧 Changes Made
• Updated the Live Activity widget timer UI to render directly from the workout start date using `Text(timerInterval:)`, so elapsed time continues advancing while the app is inactive.
• Removed foreground-only per-second Live Activity pushes from the live strength session screen and now sync activity content when exercise entries change instead.

Files modified:
• TrainStateWidgetExtension/TrainStateWidgetExtensionLiveActivity.swift
• TrainState/Views/LiveStrengthSessionView.swift

✅ Result

The Live Activity timer now continues updating on the Lock Screen and Dynamic Island without requiring the user to reopen the app, while exercise-count and current-exercise updates still sync when the workout changes.

⸻

🗓 2026-03-16 · 17:36 (GMT+2)

🔹 Request

User requested that workout edits should not require pressing save for changes.

🔧 Changes Made
• Updated the workout edit screen toolbar to use `Close`, matching the existing autosave behavior instead of implying a separate save step.
• Updated the exercise editor sheet to describe live-applied changes instead of “ready to save,” and replaced `Cancel`/`Done` with `Close` because edits are bound and persisted automatically.
• Updated the category/subcategory picker close action to use `Close` for the same live-editing behavior.

Files modified:
• TrainState/Views/EditWorkoutView.swift
• TrainState/Views/Components/ExerciseEditorSheetView.swift
• TrainState/Views/CategorySelection/CategoryAndSubcategorySelectionView.swift

✅ Result

The editing flow now reads as live-editing rather than save-driven, which matches the existing autosave implementation and removes the need to look for a save action.

⸻

🗓 2026-03-16 · 17:44 (GMT+2)

🔹 Request

User requested that if there is no workout on the same day, the app should not ask whether to import a HealthKit workout as new or attach it to an existing one.

🔧 Changes Made
• Updated the HealthKit row tap action in `WorkoutListView` to import immediately when there are no same-day workouts available.
• Narrowed attach candidates to same-day workouts only, removing the prior fallback that suggested recent workouts from other days.

Files modified:
• TrainState/Views/WorkoutListView.swift

✅ Result

HealthKit imports now skip the attach/import choice when there is no workout on that day, and attachment is only offered when there is a same-day workout to attach to.

⸻

🗓 2026-03-16 · 17:23 (GMT+2)

🔹 Request

User requested that attaching a HealthKit workout should adjust the time of the workout.

🔧 Changes Made
• Added a regression-safe initializer to `HealthKitRecentWorkoutMenuItem` so attach behavior can be tested directly without a live `HKWorkout`.
• Added a focused unit test covering `HealthKitRecentWorkoutImporter.attachWorkout` to verify the attached workout inherits the HealthKit `startDate` and `duration`.

Files modified:
• TrainState/Services/HealthKitRecentWorkoutImporter.swift
• TrainStateTests/HealthKitRecentWorkoutImporterTests.swift

✅ Result

The attach flow is now explicitly covered by tests, protecting the expected behavior that linked workouts shift to the HealthKit session time and duration.
